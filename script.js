function sendRequest() {
    var inputText = document.getElementById("inputText").value;

    // Send inputText to model and handle response
    // Example: You can use Fetch API to send a POST request to your model's API endpoint
    // Example: fetch('/api/predict', { method: 'POST', body: JSON.stringify({ inputText }) })
    //            .then(response => response.json())
    //            .then(data => document.getElementById("output").innerText = data.output);
}
